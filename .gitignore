# live-build build artifacts
binary/
binary.log
build.log
cache/
chroot/
chroot.files
chroot.packages.install
chroot.packages.live
.build/

# ISO output
*.iso
*.img

# GPG keys (fetched by auto/config, not stored in git)
config/archives/*.gpg

# Misc
*.tmp
*~
.DS_Store
