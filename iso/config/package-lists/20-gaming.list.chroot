# --- Steam input / controller support ---
steam-devices

# --- GameMode daemon (i386 libs via hook 0008-enable-i386) ---
gamemode
libgamemode0
libgamemodeauto0

# --- Gamescope (Steam Big Picture session) ---
gamescope

# --- Performance overlay ---
mangohud

# (winetricks installed via hook 0088 - not in forky right now)
