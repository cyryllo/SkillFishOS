# --- Steam input / controller support ---
steam-devices

# --- GameMode daemon (amd64; i386 libs installed by hook 0008-enable-i386) ---
gamemode
libgamemode0
libgamemodeauto0

# --- Gamescope (Steam Big Picture session) ---
gamescope

# --- Performance overlay (amd64; i386 variant via hook 0008) ---
mangohud

# --- Proton/Wine dependencies ---
winetricks
